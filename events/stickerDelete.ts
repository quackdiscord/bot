import { Events } from "discord.js";

async function execute(sticker: any) {
    // coming later
}

const data = {
    name: Events.GuildStickerDelete,
    once: false,
    execute
};

export { data };
